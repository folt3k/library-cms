export interface Borrow {
  book: number;
  borrow_date: string;
  id: number;
  is_penalty: boolean;
  is_returned: boolean;
  penalty_amount: number;
  return_date: string;
  returned_date: string;
}
